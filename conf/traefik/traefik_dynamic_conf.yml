http:
  middlewares:
    redirect-to-https:
      redirectScheme:
        scheme: "https"
        permanent: true

tls:
  certificates:
    - certFile: "/certs/_wildcard.docker.localhost.pem"
      keyFile: "/certs/_wildcard.docker.localhost-key.pem"
